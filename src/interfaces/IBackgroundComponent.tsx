interface IBackgroundComponent {
    startDate: string;
    endDate: string;
    position: string;
    place: string;
    description: string;
    isOne?: boolean;
    isLast?: boolean;
}

export default IBackgroundComponent;